const month = ['August', 'September', 'October'];
const firstElement = month[0];
const lastElement = month[month.length - 1];

console.log(firstElement); // 'August'
console.log(lastElement); // 'October'

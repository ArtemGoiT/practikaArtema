const month = new Array(3);
const monthLength = month.length;

console.log(month); // [ <3 empty items> ] OR [undefined, undefined, undefined]
console.log(monthLength); // 3

// const month = new Array(3.5); // RangeError: Invalid array length
